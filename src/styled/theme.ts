export type Theme = {
  heights: {
    appHeader: string
    appFooter: string
    breadcrumb: string
  }
}

const theme: Theme = {
  heights: {
    appHeader: '56px',
    appFooter: '44px',
    breadcrumb: '44px',
  },
}

export default theme
